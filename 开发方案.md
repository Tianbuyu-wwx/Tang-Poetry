# 石湖诗社平台 · 开发方案

> 将现有《全唐诗》静态鉴赏站改造为「石湖诗社」平台的可行性分析与落地路线。
> 最后更新：2026-08-05。

---

## 一、可行性结论

**高度可行，但定位要准。** 现有项目是"读旧体诗"体验极好的纯静态站；它**不是社区平台**（无后端、无写入、无互动）。正确做法：把它当作**前端展示与发布底座**，再用极轻量的方式叠加"投稿"和"互动"，不推翻重造带后端的系统。

### 现有可复用优势
- 古典诗渲染成熟：诗句分句、题解/注释/赏析/古籍赏评分栏、名句、动态 SEO/OG、按需懒加载。
- 数据模型已预留 `curated_` 命名空间（`page-poem.js:73` 的 `poemShardName`），天然容纳社员作品。
- 注解体系（题解·注释·赏析·古籍赏评）完美适配旧体诗。
- 纯静态部署 → 零运维、低成本，适合小诗社。
- `verify_all.py` 可作 CI 质量门，防回归。

### 需补齐的关键缺口
- 无后端、无写入路径（投稿/审核无入口）。
- 朝代硬编码为"唐"（`page-poem.js:273`），社员为当代人需泛化。
- 缺"社"专属内容类型（社课/唱和/社刊/名录/公告）。
- 无互动（评论/点赞/收藏）。
- 作者仅为字符串（`record[1]`），无社员身份与档案关联。

---

## 二、已确认的关键决策

1. **全唐诗保留为"经典库"**（只读公版，作范本），社员作品为并列的"社库"（`curated_` 命名空间）。
2. **社员诗体两者都有**（旧体诗 + 现代诗）→ 渲染层需兼顾：旧体带体裁/平仄展示，现代诗纯分行、弱化体裁。
3. **投稿层**推荐 Git CMS（Decap/Static CMS）；**互动层**推荐 Waline（可自托管，中文诗社友好，带邮件通知）或 Giscus（GitHub Discussions 驱动）。
4. 本方案存档于项目根 `开发方案.md`。

---

## 三、目标架构

```
内容源
 ├─ 经典库（全唐诗 / 历代名篇）      只读 · 公版
 └─ 社库（社员作品 curated_）         Markdown → 构建
        │
        ▼
共享渲染层（静态 · 现有 7 页 + 检索 / SEO）
 诗作 / 诗人 / 典籍 / 名句 共用一套展示与分片
   ↑↘                ↙↑
提交层            互动层
Git CMS /         Waline / Giscus
轻量后端          评论 · 唱和 · 点赞
        │
        ▼
社务层（Markdown 驱动）：社课 · 唱和 · 社刊 · 名录 · 公告
        │
        ▼
静态托管 + CI 自动重建（GitHub Pages / Vercel / Cloudflare Pages）
```

只有"投稿"和"互动"需要少量服务端，且均可走免费/廉价无服务器方案，不破坏静态托管的简单性。

---

## 四、数据模型适配

诗作为 11 字段定长数组（`page-poem.js:259`）：
`[标题, 作者, 体裁, 年代, 出处, 诗句, 题解, 注释, 赏析, 名句, 出处溯源]`。

- **社员作品**：复用 `curated_` 前缀写入同一契约。现代诗时 `体裁` 置"新诗/现代诗"、相关古体专属展示降级。
- **朝代泛化**：`page-poem.js:273` 当前对无显式朝代者一律显示"唐"，改为按档案取，默认值改为"今"。
- **社员档案**：作者当前仅为字符串，需新增"社员档案"数据（简介/辈分/主打体裁），与唐代诗人档案区分（可新增 `MEMBERS_DATA`，或在 `POETS_INDEX` 增加"是否社员"标记）。
- **社务关联**：社课/唱和/社刊通过新增可选字段（如 `record` 扩展或独立索引文件）建立"题目→作品""原唱→和作"关系，不改 11 字段核心契约。

---

## 五、六阶段落地路线

### P0 · 定位与品牌重塑（纯前端，零风险）
- 抽离"全唐诗 · 鉴赏全集"等硬编码为单一品牌配置层（站名/页脚/OG/SVG 图标）。
- 涉及 `page-poem.js:53,341`、`index.html`、`about.html`、`navigation.html`、`site-meta.js`、`register-sw.js` 等。
- 产出：可切换为"石湖诗社"风格的站点雏形。
- 验证：本地 `python -m http.server` 浏览 7 页，无残留"全唐诗"字样（经典库区域保留书名即可）。

### P1 · 数据模型扩展（仍纯前端）
- 落地 `curated_` 社库：新增社员作品示例数据，确保分片加载、渲染、SEO 全通。
- 朝代泛化（`page-poem.js:273`）。
- 新增社员档案数据 + 社员名录页雏形。
- 现代诗展示分支：体裁为"新诗/现代诗"时弱化体裁/平仄，纯分行渲染。
- 验证：`verify_all.py` 通过（扩展断言兼容 curated_）；`verify_frontend.js` 通过。

### P2 · 投稿工作流（引入轻量服务端）
- 接入 **Git CMS（Decap/Static CMS）**：社员在网页编辑器写 Markdown（frontmatter 含 标题/作者/体裁/年代/正文/题解/注释/赏析）→ 提交 PR → 社长审核合并 → CI 自动 `import_docs.py` 重建并部署。
- 或备选：轻量后端（Cloudflare Workers / 云函数）接收投稿写入队列，构建期消费。
- 新增脚本 `scripts/markdown_to_curated.py`：Markdown → `curated_` 11 字段记录 → 分片。
- 验证：模拟一次 PR 合并触发重建，新作上站。

### P3 · 互动层
- 接入 **Waline**（自托管或 Serverless）或 **Giscus**，在诗作/社刊页嵌入评论，承载唱和点评与读者交流。
- 验证：评论可发表、可邮件通知、移动端可用。

### P4 · 社务内容（Markdown 驱动）
- 社课页：列出题目 + 该题目下所有社员作品。
- 唱和页：按"原唱→和作"关系聚合。
- 社刊页：按"期"分组（复用 source-book 式结构）。
- 社员名录页：社员档案 + 代表作出处。
- 公告/活动页：Markdown 驱动的简单资讯流。
- 验证：各页路由与渲染正确，`verify_all.py` 扩展断言通过。

### P5 · 部署运营
- CI：每次合并自动重建 + 跑 `verify_all.py` / `verify_frontend.js` 作质量门。
- 压缩：静态托管开启 brotli（67MB → 传输约 10MB 级）。
- 自有域名 + HTTPS；Service Worker 缓存策略复核。
- 验证：生产环境加载性能与可用性。

---

## 六、风险与注意

- **id 命名空间**：`curated_` 与 `open_` 共用 `Math.floor(id/500)` 分桶，需规划社员作品 id 分配，避免与未来扩展冲突（可预留 `curated_` 高位段）。
- **内容审核**：社员投稿需审核（质量与授权）。Git CMS 的 PR 审核机制天然满足；若用后端须自带审核流。
- **规模**：全唐诗 5.7 万首 / 67MB 是主体，社员内容极小，无扩展性问题。
- **现代诗渲染**：现有按标点分句逻辑可用，但"体裁/平仄"展示需降级，避免空字段干扰。
- **重建守卫**：批量分片重建会触发安全删除守卫；用 `atomic_write` 覆盖式重建（`reset_generated_directory` 打补丁为 no-op），勿用 `git rm` 批量删（详见项目记忆）。

---

## 七、建议起步

从 **P0 品牌重塑** 动手——不依赖任何后端决策，改完即可看到"石湖诗社"风格站点雏形，再逐步叠加 P1→P5。

---

## 八、详细开发计划（分阶段任务分解）

> P0 已完成（2026-08-05，commit `43a2023`）。以下 P1–P5 为详细任务，每项标注涉及文件/函数、验收标准、依赖顺序与预估工时。
> 工时以"单人兼职推进"为尺度，仅供参考。

### P0 · 品牌配置化（✅ 已完成）
- **状态**：已完成。新增 `website/assets/js/brand.js` 单一数据源 + `applyBrand()`；7 个页面 `<title>`/Logo/印章/页脚 + `page-*` 的 `setOg`/`document.title` 全部改读 `window.BRAND`；保留作为底本事实描述的"全唐诗"。
- **验收**：`node --check` 全过；`verify_frontend.js` 解析 222 段脚本通过；全站无"全唐诗 · 鉴赏全集 / Complete Tang Poems / Seal of Tang"残留。

---

### P1 · 数据模型扩展（纯前端，约 3–5 天）
**目标**：让"社库"真实上站，并让渲染层兼容当代社员与现代诗。

- **P1.1 社库落地（首轮已完成，2026-08-05，commit 见下）**
  - 关键命名决策：**社员作品用 `member_` 前缀，社员档案用 `member_poet_` 前缀**，与既有的 `curated_`（古典「补遗诗」，`SITE_META.supplements` 共 66 首，不可删）和 `open_`（全唐诗）三者并列，共用同一分桶路由。
  - 新增 `scripts/build_curated.py`：读 `docs/curated/*.md`（YAML frontmatter，字段=标题/作者/体裁/年代/出处/正文/题解/注释/赏析/名句/出处溯源）→ 11 字段记录 `member_N`；读 `docs/curated/members.json` → `POETS_DATA` 的 `member_poet_XXXX`；并入后调用 `build_frontend_assets.build_assets()`（`reset_generated_directory` 打 no-op 避开安全删除守卫）全量重建分片与索引；`site-meta` 计数由真实数据重算（非增量，避免误差）。依赖 pyyaml（managed venv）。
  - `page-poem.js` / `build_frontend_assets.py` 的 `poemShardName` 正则已扩展为 `^(open|curated|member)_(\d+)$`。
- **P1.2 朝代泛化（已完成）**：`page-poem.js` 朝代默认由"唐"改为"今"；`build_frontend_assets.build_poet_assets` 对 `isMember` 诗人直接取 `dynasty`（如"今"）而非套用唐代分期；面包屑"卷"→"诗作"。
- **P1.3 现代诗渲染分支（已完成）**：`renderPoem` 诗句循环支持空段（stanza gap，`poem.css` 加 `.stanza-gap`）；`体裁 ∈ {新诗,现代诗,自由诗,白话诗}` 时隐藏"体裁" meta。
- **P1.4 社员档案 / 名录页（已完成，2026-08-05）**
  - `POETS_INDEX` 条目追加第 10 位 `isMember` 标记位；同一轮循环内额外产出 `members-index.js`（`window.MEMBERS_INDEX`，仅社员，**内联作品清单**，使名录页单文件自足、不必加载 3,658 人的全量索引）。
  - 新增 `website/members.html` + `assets/js/page-members.js`：社员卡片（印章／姓名／身份／时代／作品数／小传／作品直达）、姓名与篇名双维检索、投稿入口 CTA。
  - 导航接入：`navigation.html`／`about.html`／`poets.html`／`sources.html`／`poem.html`／`poet.html` 顶栏加"社员"，`index.html` 主视觉下加"社员名录 · 关于本站"。
  - `service-worker.js` 预缓存加 `members.html`，缓存版本 v4→v5；`/admin` 路径绕过缓存。
- **P1.5 端到端样例（已完成）**：已收录 3 首社员作品（七绝《石湖春晓》、五律《雨过石湖》、现代诗《湖畔》）+ 2 份社员档案（石湖客、若蘅），双校验门全绿。
- **验收（首轮）**：`verify_all.py`（57,676 首 / 3,658 诗人 / 5,870 注解）+ `verify_frontend.js`（223 段脚本）全绿；`member_*` 诗作页、检索、诗人索引均落地；现代诗分句/stanza gap 正确。
- **验收（P1.4）**：`verify_all.py` 新增 5 项断言（名录索引与 `isMember` 一致、内联作品与分片一致、`member_` 命名空间、名录页只加载轻量索引、主要页面均有社员入口）全绿。
- **依赖/顺序**：P1.1 → P1.2/P1.3（可并行）→ P1.4 → P1.5。**P1 全部完成。**

---

### P2 · 投稿工作流（引入轻量服务端，约 4–7 天）
**目标**：社员能在网页后台投稿，社长审核后自动上站。

- **P2.1 Git CMS（Decap）接入（已完成，2026-08-05）**
  - `website/admin/index.html`（Decap CMS 3.15.x，CDN 引入，带载入占位）+ `website/admin/config.yml`。
  - **数据布局改为 folder collection**：`docs/curated/members.json` 拆为 `docs/curated/members/<短名>.md`，诗作移入 `docs/curated/poems/<短名>.md`，与 Decap 的两个集合一一对应。`build_curated.py` 相应重写。
  - 文件名策略：frontmatter 增 `id` 字段（ASCII 短名），`slug: "{{fields.id}}"`，避免中文文件名；构建期校验 `id` 与文件名一致。
  - `publish_mode: editorial_workflow`（草稿→待审→发布，对应 PR），`local_backend: true`（`npx decap-server` 可离线试用）。
  - 字段严格对齐 11 字段契约；解析层 `_pair()` 同时兼容 Decap 的对象列表（`{term,gloss}` / `{line,comment}`）与手写底稿的成对数组，已做往返验证。
  - **链接稳定性**：新增 `docs/curated/poem-ids.json` 持久化「文件名 → 数字 id」，号码只增不减，杜绝新增投稿挪动既有作品 URL。
  - 文档：`docs/投稿后台使用说明.md`（社员投稿指南 + 社务审稿上站 + 部署前置项 + 本地试用 + FAQ）。
- **P2.2 后端对接（暂缓，2026-08-05 决定先只用本地模式）**
  - 现阶段走 `local_backend`：`npx decap-server` + 本地静态服务器即可跑完整投稿流程，零成本、零配置，先把内容与流程磨顺。
  - 接线上时须办：`config.yml` 的 `backend.repo` 换成真实仓库（部署工作流会自动注入，源码可保持占位）；GitHub 后端需 OAuth 中转，Netlify（内置 Identity）／Cloudflare Worker 自建／官方 `decap-cms-github-oauth-provider` 三选一。
  - 前置清单见 `docs/部署与CI说明.md` 第五节。
- **P2.3 CI 自动重建（已完成，2026-08-05）**
  - `.github/workflows/verify.yml`（投稿校验）：PR 触发，重建 → 双校验门 → 生成投稿体检报告贴回 PR（sticky 评论，重复推送只更新同一条）。只读不写，不部署。`if: always()` 保证构建失败时照样出报告。
  - `.github/workflows/deploy.yml`（构建并部署）：推送 `main`/`master` 触发（两者都监听，防改名后静默失效），重建 → 双校验门 → 持久化编号注册表 → 注入仓库名到 `admin/config.yml` → 部署 GitHub Pages。`concurrency: pages` 串行，避免并发上线后发先至。
  - **产物策略（关键决策）**：CI 重建但**不回写大产物**。依据是实测——全量重建仅 2.3–3.5 秒且确定性（重跑零 diff），而 `poems-data.js` 是 29.8 MB 单行 JS，每回写一次就在仓库压出约 **11.1 MB** 新 blob。唯一持久化的是几百字节的 `poem-ids.json`（URL 稳定性凭据）。需要对齐仓库与线上时，手动触发并勾选 `sync_artifacts`。
  - `scripts/review_submission.py`（投稿体检报告）：字段完整度、作者是否在册、分配到的编号与上站地址、**格律形制校验**。形制按「句」（标点切分）统计而非按行——旧体诗一联一行，八句五律通常只占 4 行，按行统计会误判。实测能抓出「标七绝却写五言」。
  - `requirements-build.txt`：CI 最小依赖集，唯一第三方依赖 `pyyaml`；构建链路无 Windows 特定代码，Linux runner 可跑。
  - 文档：`docs/部署与CI说明.md`（事实源契约、两个工作流详解、体检报告用法、本地模式操作、接线上清单）。
- **验收**：已用 `git worktree` 建纯净检出模拟 CI 全链路——从零检出 → 构建（2.3s）→ 双校验门全绿 → 模拟一次真实投稿 PR → 体检报告正确输出 → 新稿分到 `member_3` 且既有 0/1/2 号纹丝未动（URL 无漂移）→ 57,677 首（+1）落入分片。端到端上线待 P2.2 接入远端仓库后验收。
- **依赖/顺序**：P2.1 ✅ → P2.3 ✅（工作流已就绪，推上 GitHub 即生效）→ P2.2（用户决定接线上时再办）。

---

### P3 · 互动层（约 2–4 天）
**目标**：诗作/社刊可评论、可唱和点评。

- **P3.1 Waline 接入（推荐）**
  - 部署 Waline 服务端（Vercel + 数据存 LeanCloud / MySQL / SQLite）；在 `poem.html` 及 `curated_` 诗作页、`社刊`页注入 Waline 容器 + 脚本（`data-serverURL`）。
  - 开启邮件通知（社员唱和/点评可触达）。
- **P3.2 或 Giscus（备选）**：基于 GitHub Discussions，注入 giscus 脚本，零自建服务端但需公开仓库。
- **P3.3 唱和关系**：利用 Waline 的 `@`/引用，或在 P4 用独立"和作"字段显式聚合。
- **验收**：评论可发表、移动端可用、邮件通知送达。
- **依赖/顺序**：可独立于 P2 先做（Waline 不依赖投稿流）；推荐与 P2 并行。

---

### P4 · 社务内容（Markdown 驱动，约 5–8 天）
**目标**：补齐"诗社"专属内容形态。
**状态**：P4.1 社课 / P4.3 社刊 / P4.5 公告 + 社务中枢 `society.html` **已完成（2026-08-05）**。实现要点：新增独立构建 `scripts/build_society.py`，读 `docs/lessons.json` / `docs/news/*.md` / `docs/periodicals/*.json`，输出 `lessons-index.js` / `news-index.js` / `periodicals-index.js`，与核心诗库管线**完全解耦**（不触碰 poems-data.js，不影响 57k 诗作重建）；作品以 `member_` id 交叉链接 `poem.html?id=`。P4.2 唱和页与 P4.4 名录增强留待后续（唱和归 P3 互动范畴，名录增强为可选优化）。

- **P4.1 社课页 `lessons.html`**：数据源 `docs/lessons.json`（题目/起止/要求）→ 列出题目 + 该题目下所有 `member_` 作品（按 id 关联，不改 11 字段核心契约）。
- **P4.2 唱和页 `bonds.html`**：按 `original_id` / `harmony_of` 聚合"原唱→和作"。
- **P4.3 社刊页**：复用 source-book 结构（`docs/curated_periodicals/*.json`），直接复用 `sources.html` 渲染。
- **P4.4 社员名录增强**：`members.html` 基础版已于 P1.4 上线；此处补社课/唱和维度的聚合（如"该社员参与的社课"、"其原唱与和作"）与入社时间等档案字段。
- **P4.5 公告/活动页 `news.html`**：`docs/news/*.md` 驱动的简单资讯流。
- **验收**：各页路由与渲染正确；`verify_all.py` 扩展断言（社课/唱和/社刊索引完整）通过。
- **依赖/顺序**：P1 完成即可启动；P4.1/P4.2 依赖 P1.1 的扩展字段约定。

---

### P5 · 部署运营（约 2–3 天 + 持续）
**目标**：稳定运行、可观测、易维护。

- **P5.1 CI 流水线**：GitHub Actions（build → `verify_all.py` → `verify_frontend.js` → deploy）；失败阻断合并。
- **P5.2 压缩与缓存**：静态托管开启 brotli（67MB → 传输约 10MB 级）；配置长期缓存头（带 `?v=` 指纹的资源）。
- **P5.3 域名与安全**：自有域名 + HTTPS；`service-worker.js` 版本号 bump 并复核缓存策略（避免旧 SW 缓存导致品牌/内容不更新）。
- **P5.4 观测**：`web-vitals.js` 已埋点；可选隐私友好统计（GoatCounter / Umami）。
- **验收**：生产环境加载性能达标、可用性正常、重建可一键触发。

---

### 九、里程碑与排期建议

| 里程碑 | 内容 | 前置 | 预估 |
|---|---|---|---|
| M0 | P0 品牌重塑 | — | ✅ 完成 |
| M1 | P1 社库 + 社员档案 + 现代诗渲染 | M0 | 3–5 天 |
| M2 | P2 投稿工作流（Decap + CI） | M1 | 4–7 天 |
| M3 | P3 互动层（Waline） | M0/M1 | 2–4 天 |
| M4 | P4 社务内容（社课/唱和/社刊/名录/公告） | M1 | 5–8 天 |
| M5 | P5 部署运营 | M1–M4 | 2–3 天 |

**推荐推进节奏**：M0 → M1（必须先夯实数据底座）→ M2/M3 并行 → M4 → M5。M3 可与 M2 并行以尽早获得反馈。

**关键约束回顾**：
- 批量分片重建会触发安全删除守卫，用 `atomic_write` 覆盖式重建（`reset_generated_directory` 打补丁为 no-op），勿用 `git rm` 批量删（详见项目记忆 2026-08-05）。
- `curated_` 与 `open_` 共用分桶逻辑，需规划社员作品 id 高位段，避免冲突。
- 投稿必须经审核（质量与授权）；Git CMS 的 PR 审核天然满足。
