# 全唐诗静态鉴赏站 · 项目状态与优化记录

> 本文档为**当前真实状态**记录（取代原先过时的对话日志）。所有数字均来自
> `verify_all.py` / `verify_frontend.js` 实测与磁盘统计，最后更新：2026-07-29。

---

## 一、项目定位

纯静态、无后端的《全唐诗》鉴赏站。以 `docs/` 公版语料为源，用 `scripts/` 的
Python 管线（统一繁简、归并作者、挂接古籍注评）生成 `website/` 的 7 个 HTML
页面 + 按需加载的 JS 数据分片。部署只需静态托管 `website/`。

## 二、当前核心数据（实测）

| 指标 | 现值 |
|---|---|
| 诗作 | 57,673 首 |
| 诗人 | 3,656 位 |
| 带注解诗 | 5,758 首 |
| 名句 | 558 条 |
| 公版典籍 | 17 部 |
| 典籍章节 | 483 章 |
| 前端资源版本 | `?v=13` |

## 三、架构与数据管线

```
docs/ (公版语料)
   │  import_docs.py ──→ scripts/_work/poems-data.js  (完整诗库·构建中间产物)
   │                         │
   │                         ▼
   │                   build_frontend_assets.py
   │                         │
   ▼                         ▼
website/  ←── 轻量索引 + 按需分片（poem-shards / poet-work-shards / source-books）
```

- **`scripts/_work/poems-data.js`（~30MB）**：完整诗库，**仅构建期使用**
  （import_docs / build_frontend_assets / fetch_wiki 读写，verify_all 校验）。
  **不随站点托管**——浏览器侧通过按需分片加载，从不下载它。
- 运行时仅加载：`poems-index.js`(5.2M)、`poets-data.js`(2.5M)、
  `sources-data.js`(13M)、各分片与 UI 脚本。

## 四、前端资产现状

- `website/` 总体积：**67 MB**（清理孤儿中间产物前为 97 MB，见下方 P1-1）。
- 最大文件：`poems-index.js` 5.2M、`sources-data.js` 13M、`sources-data.js`
  下属 `source-books/*` 单部最大 3.0M；`poem-shards/*` 117 个、`poet-work-shards/*` 64 个。
- 已落实的前端优化：字体 `display=swap`、动态 SEO/OG meta、移除 no-cache、
  图片懒加载、`defer`/`preload`、检索高亮、古籍注释分句、返回顶部、平滑滚动。
- P3 已落地：`service-worker.js` + `register-sw.js` + `web-vitals.js` 接入全部 7 页。

## 五、已完成工作

### P0（2026-07-27）
- 清理根目录 24 个 `tmp_*` 原型脚本 → `scripts/_archive/experiments/`。
- 修复 `verify_all.py` 两处失败：①硬编码旧机器绝对路径（随 tmp 移出扫描范围消除）；
  ②诗人路由静态检查误报（改为同时检查 `page-poets.js` 运行时链接）。
- 初始化 git 仓库（全局身份 `天不语 <2364309541@qq.com>`），两次提交夯实现状。

### P1-1（2026-07-29）— 前端资产瘦身
- 发现 `poems-data.js`(30M) 是**孤儿**：浏览器从不加载，仅构建管线使用。
- 将其从 `website/assets/js/` 迁出至 `scripts/_work/`，更新
  `import_docs.py` / `build_frontend_assets.py` / `fetch_wiki.py` /
  `verify_all.py` 及 7 个 crawl 脚本路径引用。
- **效果：`website/` 97 MB → 67 MB（减少 31%）**，两个校验仍全绿。

### P1-2（2026-07-29）— 校验审查
- 复核 `verify_all.py` 全部断言：数据完整性对比 meta、SPA `?id=` 路由、
  不预载大文件等校验均有效。**除 P0 已修复的诗人路由误报外，无其余过严误报。**

## 六、待办 / 后续计划

| 优先级 | 事项 | 说明 |
|---|---|---|
| **P2-4** | 古典诗话评点从原型转正 | 将 `scripts/_archive/experiments` 下 `tmp_诗人玉屑`、`tmp_人间词话`、`tmp_probe_modern_books` 的古典评点逻辑固化进 `import_docs` 主流程，提升注解覆盖率并避免回归为游离原型。 |
| **P3-5** | 仓库瘦身（需确认） | 初始提交约 4095 文件含 `scripts/crawl/tang_dict/` 近千抓取 HTML。可加 `.gitignore` 排除抓取产物并清理提交；会改写已提交内容，**需用户确认后执行**。 |

### 备注：进一步压缩的可选方向
- 当前 67MB 中 `sources-data.js`(13M) 与 `poem-shards`(~50M) 为浏览器必需数据，
  磁盘层面再降需改动数据格式（如紧凑二进制/列式）；传输层面若托管方支持
  gzip/brotli 可降约 5–7×，但 `python -m http.server` 本地托管不压缩，
  如需受益须改用支持压缩的静态服务器。

---

*本文件取代原先 823 行对话转录日志（其数字已过时，记载 13 典籍 / 4206 注解，
实际已为 17 典籍 / 5758 注解）。*
